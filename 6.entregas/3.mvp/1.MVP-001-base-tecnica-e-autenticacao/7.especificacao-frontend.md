# MVP-001 — Especificação Frontend

Não há alteração de frontend nesta entrega.

Após a proteção do Backend, o Frontend precisa enviar `Authorization: Bearer <token>` em chamadas autenticadas. Interceptor HTTP, leitura de `sessionStorage.token` e tratamento de HTTP 401 pertencem a entrega posterior.
